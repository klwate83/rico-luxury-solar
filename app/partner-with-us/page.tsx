"use client";
import PageHero from "@/components/PageHero";
import { FormEvent, useState } from "react";
export default function Page(){ const [sent,setSent]=useState(false); async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();setSent(true)} return <><PageHero eyebrow="Partner With Us" title="Apply to join the RICO solar partner network." description="The launch network focuses on vetted solar installers serving Cape Town and selected Western Cape areas." cta="Start Partner Application"/><section className="section-pad bg-mist"><div className="container-shell max-w-4xl">{sent?<div className="card text-center"><h2 className="text-2xl font-bold text-navy">Application captured</h2><p className="mt-3 text-slate-600">This prototype requires CRM or storage integration before production use.</p></div>:<form onSubmit={submit} className="card grid gap-5"><div className="grid gap-4 md:grid-cols-2"><input required className="input" placeholder="Registered company name"/><input required className="input" placeholder="Contact person"/><input required type="email" className="input" placeholder="Business email"/><input required className="input" placeholder="Mobile number"/><input className="input" placeholder="Website"/><input className="input" placeholder="Office location"/></div><textarea className="input min-h-32" placeholder="Areas serviced, capabilities, accreditations and installation capacity"/><label className="flex gap-3 text-sm"><input required type="checkbox"/> I consent to the RICO partner-vetting process.</label><button className="btn-primary">Submit Partner Application</button><p className="text-sm text-slate-500">Submitting an application does not automatically approve a business as a RICO partner.</p></form>}</div></section></> }
